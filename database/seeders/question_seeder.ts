import { BaseSeeder } from '@adonisjs/lucid/seeders'

export default class extends BaseSeeder {
  async run() {
    // Write your database queries inside the run method


    // const q1 = await Question.create({enonce: "Quelle est la réponse universelle ?"})
    // const q2 = await Question.create({enonce: "Quel est le meilleur framework JS ?"})
  }
}